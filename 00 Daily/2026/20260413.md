# 2026-04-13

---

## Session Log - 17:30

**Focus:** RVC voice retraining → AI OS full implementation (Skills, Sandbox, AgentBuilder, Catalog) → audit → agents v2 → UI + Tauri

### Completed
- Trained jarvis_v2 RVC model (400 epochs, 52-file Sound Pack)
- Exported jarvis_v2 + jarvis_v2_best to ONNX (105 MB each)
- Fixed 4 critical bugs in rvc_onnx.py (HuBERT 2x repeat, F0 mel encoding, transpose, speed)
- Tuned voice: pitch +5, EQ matched to Sound Pack (FINAL1 approved)
- Switched to DirectML GPU — RVC 0.57s (3.2x speedup)
- Merged ONNX RVC into TTS server (single process, no WSL)
- Implemented AI OS Skill Engine: 5 templates (tracker/reminder/monitor/notifier/logger), SkillExecutor, dynamic cron
- Implemented Sandbox: structured permissions, NetworkProxy via JSON-RPC, RateLimiter, filesystem enforcement
- Implemented AgentBuilder v2: intent classifier, safety gate (AST), LLM code generator, deployer, wizard
- Implemented Cloud Catalog: .kali-agent package format, Supabase client, installer
- Full audit: found 7 critical + 10 important issues, all fixed
- Upgraded smart-home agent → Home Assistant REST API
- Upgraded coding agent → Claude API integration
- Built Agent Store UI (React component)
- Configured Tauri desktop: system tray, Ctrl+Space hotkey, CSP
- Created VISION.md project manifest
- 464 tests passing

### Decisions Made
| Decision | Rationale |
|----------|-----------|
| Skills (YAML) + Agents (code) dual model | Users create simple things without code risk; complex things with LLM + sandbox |
| NemoClaw-inspired sandbox via JSON-RPC | Subprocess agents can't import requests/urllib; all HTTP through kernel proxy |
| Permission-based (not strict sandbox) | Like smartphone permissions — user approves per agent |
| Cloud Catalog on Supabase | PostgreSQL + Auth + Storage, free tier sufficient for MVP |
| Karpathy coding rules as default | Prevents LLM overengineering — simplicity first, surgical changes |
| DirectML over CUDA for RVC | No CUDA toolkit needed on Windows, RTX 5070 works with DirectML |

### Files Created/Modified
- [[VISION.md]] - Project manifest (mission, architecture, roadmap, UX scenarios)
- [[docs/superpowers/specs/2026-04-13-kali-ai-os-design.md]] - AI OS technical spec
- [[kernel/skill_executor.py]] - In-process skill runner
- [[kernel/skill_templates/]] - 5 template implementations
- [[kernel/sandbox/]] - Permission enforcer, network proxy, rate limiter
- [[kernel/builder/]] - Intent classifier, safety gate, generators, deployer, wizard
- [[kernel/catalog/]] - Package format, Supabase client, installer
- [[services/tts/server.py]] - Silero + ONNX RVC integrated TTS
- [[services/tts/rvc_onnx.py]] - ONNX RVC inference engine
- [[ui/src/components/AgentStore/]] - Agent Store UI

---

## Session Log - 23:59

**Focus:** Wire ALL UI to real backend → audit fixes → server consolidation → release pipeline

### Completed
- **Dashboard widgets connected to real data**: Tasks, Calendar, Weather (Open-Meteo 6.6°C live), Sleep, Spending, Energy — all via /agents/{name}/execute route
- **Created WeatherWidget.tsx** — new 7th dashboard widget
- **Fixed /agents/{name}/execute** — route didn't exist, added + auto-approve built-in agents (PermissionEnforcer was blocking everything)
- **Fixed weather field mismatch** — backend returns `temperature_c`, frontend expected `temp_c`
- **Voice controls wired** — J●/J○ toggle button calls voiceStart/voiceStop
- **Builder UI created** — classify intent + create skill from text input, embedded in Agent Store
- **Catalog local search** — works without Supabase, scans agents/*/manifest.yaml
- **VoiceVisualizer rewritten** — Web Audio API with real microphone FFT data on canvas (replaced fake CSS animation)
- **Agent Store Install** — shows "Installed" badge for local packages
- **Server consolidation** — TTS merged into kernel (kernel/voice/tts_engine.py), one process on port 3005, no separate Flask server
- **Frontend updated** — ChatInput calls 3005 instead of 3002
- **Voice pipeline deps installed** — faster-whisper, sounddevice, openwakeword (all verified working)
- **Tauri desktop built** — 13MB exe with system tray, Ctrl+Space, auto-start backend
- **Release pipeline created**:
  - scripts/build_backend.py (PyInstaller → kali-backend.exe)
  - scripts/installer.nsi (NSIS → KALI-Setup-0.1.0.exe)
  - scripts/build_release.bat (one-click full build)
  - kernel/model_downloader.py (download ONNX models on first run)
  - src-tauri/lib.rs: auto-start/stop kali-backend.exe + health check
- **464 tests passing, 0 hardcode, 0 dead buttons**

### Decisions Made
| Decision | Rationale |
|----------|-----------|
| Consolidate TTS into kernel | One process for distribution, simpler for users |
| PyInstaller for backend | Single .exe, no Python installation needed |
| NSIS installer | "Далее-Далее-Готово" for non-technical users |
| Auto-start backend from Tauri | User never sees terminal or command line |
| Model download on first run | Keeps installer small (~50MB), models (~700MB) download once |
| Local catalog as Supabase fallback | App works fully offline, cloud enhances |
| Auto-approve built-in agents | System agents shouldn't need user permission |

### Files Created/Modified
- [[kernel/voice/tts_engine.py]] - In-process TTS (Silero + RVC ONNX + EQ)
- [[kernel/model_downloader.py]] - First-run model downloader
- [[ui/src/components/Builder/Builder.tsx]] - Agent/skill creation UI
- [[ui/src/components/Dashboard/widgets/WeatherWidget.tsx]] - Real weather widget
- [[ui/src/components/VoiceVisualizer/VoiceVisualizer.tsx]] - Web Audio FFT visualizer
- [[scripts/build_backend.py]] - PyInstaller build config
- [[scripts/installer.nsi]] - NSIS installer script
- [[scripts/build_release.bat]] - One-click release builder

### Architecture After Consolidation
```
User double-clicks KALI.exe
  → Tauri starts kali-backend.exe (invisible)
    → FastAPI kernel on :3005
      ├── /chat (LLM + agents)
      ├── /agents (9 agents, auto-loaded)
      ├── /skills (skill templates)
      ├── /tts + /synthesize (Silero + RVC ONNX in-process)
      ├── /builder (create agents/skills)
      ├── /catalog (search + install)
      └── /ws (WebSocket real-time)
  → Tauri renders React UI
    ├── Dashboard (7 widgets, real data)
    ├── Agent Panel (load/unload)
    ├── Agent Store (search + builder)
    ├── Chat (voice + text)
    └── Voice Visualizer (Web Audio FFT)
  → Ctrl+Space toggles window
  → System tray icon
```

### Next Steps
- [ ] Run full release build (scripts/build_release.bat)
- [ ] Test installer on clean Windows machine
- [ ] React Native mobile app planning
- [ ] Supabase cloud catalog deployment
- [ ] Phase 7: Smart integrations

---
