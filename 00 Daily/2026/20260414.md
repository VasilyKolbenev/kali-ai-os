# 2026-04-14

---

## Session Log - 02:30

**Focus:** Desktop packaging, installer, E2E testing — довести до "двойной клик и работает"

### Completed
- WebView2 добавлен в NSIS installer (авто-скачивание если отсутствует)
- PyInstaller backend собран: `kali-backend.exe` (293 MB)
- Tauri desktop собран: `kali-desktop.exe` (13 MB) с auto-start backend
- NSIS installer собран: `KALI-Setup-0.1.0.exe` (297 MB)
- Установщик работает: ярлык на рабочем столе, Program Files, Add/Remove Programs
- Десктоп приложение открывается с Arc Reactor аватаром
- Чат работает (через LLM-OpenAI)
- Агенты работают (погода: 6°C Moscow ✅)
- Найдена и исправлена пропущенная точка входа `if __name__ == "__main__"` с uvicorn
- Установлена зависимость `soundfile` для TTS engine
- TTS engine загружается и генерирует WAV корректно (проверено curl)
- Git push прошёл после очистки .pt файлов из истории (filter-branch)

### Нерешённые проблемы (на завтра)
- [ ] **TTS не играет в Tauri** — endpoint /synthesize возвращает WAV (200 OK), но звук не воспроизводится в приложении. Вероятно CSP `media-src` или blob handling. Нужно протестировать в браузере vs Tauri
- [ ] **Voice activation не работает** — OpenWakeWord API изменился (`wakeword_models` → другой параметр). Silero VAD: permission denied на кэш. Нужно исправить оба
- [ ] **Backend auto-start из installer** — kali-backend.exe в Program Files молча падает. PyInstaller exe нужно дебажить (вероятно не находит config/agents relative paths)
- [ ] **Фронтенд J● кнопка спамит /voice/start** — нужно debounce или проверка состояния
- [ ] **Пересобрать installer** после всех фиксов (WebView2 + CSP + TTS)

### Решения
| Решение | Обоснование |
|---------|-------------|
| find_backend() ищет в 4 местах | Работает и из dev, и из installed mode |
| WebView2 в NSIS installer | Без него Tauri не запускается |
| soundfile как зависимость | Нужен для WAV encoding в tts_engine |
| filter-branch для .pt файлов | GitHub лимит 100MB на файл |

### Самокритика (честная)
- **Халтура**: несколько раз "исправлял" файлы которые не подхватывались (git stash pop перезаписал fix, старый exe не скопировался)
- **Халтура**: TTS consolidation не проверил что soundfile в зависимостях
- **Халтура**: uvicorn entry point пропал — не заметил что create_app() нигде не вызывается
- **Халтура**: Rust lifetime ошибки исправлял по одной вместо тщательной проверки компиляции
- **Вывод**: нужно тестировать КАЖДЫЙ шаг end-to-end, а не полагаться на "должно работать"

### Файлы изменённые
- [[src-tauri/src/lib.rs]] — auto-start backend, find_backend(), stop on close
- [[src-tauri/tauri.conf.json]] — CSP media-src, убран global-shortcut из plugins
- [[scripts/installer.nsi]] — WebView2 auto-install
- [[scripts/build_backend.py]] — PyInstaller config
- [[kernel/main.py]] — добавлен uvicorn entry point
- [[kernel/model_downloader.py]] — first-run model download

### На завтра — план
1. **Починить TTS воспроизведение** — тестировать в браузере, потом в Tauri
2. **Починить voice activation** — OpenWakeWord + Silero VAD
3. **Починить PyInstaller backend** — relative paths для config/agents
4. **Пересобрать полный installer**
5. **E2E тест**: установить на чистую → запустить → чат → голос → всё работает
6. **Без халтуры**: каждый шаг проверять реально, не "должно работать"

---

## Session Log - 15:00

**Focus:** Desktop polish — Settings, encoding, Russian UI, PyInstaller fix

### Completed
- Settings: 4 LLM providers (OpenAI, Anthropic, Google/Gemini, DeepSeek) с моделями 04.2026
- Language selector (Русский/English/中文) в Settings
- Button model selectors (вместо select который не работал в WebView2)
- UTF-8 encoding fix — "Трекер воды" корректно отображается
- Dashboard: русские подписи (СОН, ЗАДАЧИ, ПОГОДА, АГЕНТЫ)
- Builder убран из Agent Store — создание через основной чат
- Backend settings: Google/DeepSeek ключи + language
- **PyInstaller root cause**: sqlite readonly в Program Files → fix: AppData
- STT: CPU mode (cublas fix), HF cache redirect
- TTS server-side playback, anti-echo, numbers-to-words, новый EQ
- JARVIS pre-recorded clips (17 mp3)
- Pre-build аудит: 7/7 OK
- VISION.md обновлён — Phases 4-7 = DONE

### Нерешённые проблемы
- [ ] Tauri + NSIS пересобрать и протестировать installed app E2E
- [ ] Voice models auto-download при первом запуске
- [ ] Верификация kali-backend.exe с AppData DB

---
