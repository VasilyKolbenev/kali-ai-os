# Opus 4.8 — implementation log (release trusted-alpha)

Дата: 2026-07-17. Ветка: `release/phase-a-desktop-alpha` (не main). Статус: **PHASE A блок 1 (OPUS-001/002) реализован + review-fix закрыт; пауза перед A3.**

Источник истины: `docs/public-launch/2026-07-17-prod-readiness-audit-opus-4.8.md`.
Порядок доверия: clean artifact/live logs > HEAD+мутирующие тесты > аудит 2026-07-17 > старые handoff > dist (никогда).

---

## 1. Git / source state

| Проверка | Ожидалось | Фактически | Статус |
|---|---|---|---|
| HEAD | e3db43c | `e3db43c` | PASS |
| origin/main | — | `e3db43c` (совпадает, расхождения нет) | PASS |
| dirty tree | — | 8 tracked M + много untracked | см. ниже |

Последние коммиты: e3db43c(docs plans) · ebcf981(fix voice chunk) · 92ddeda(docs findings) · d4391d4(fix HF_HOME STT) · c2374f1(fix installer sign code).

### Классификация dirty files
- **Generated (не трогать):** `mobile/{linux,macos,windows}/**/generated_plugin_registrant.*`, `GeneratedPluginRegistrant.swift`, `generated_plugins.cmake` — Flutter plugin codegen.
- **Lock drift (evidence P1-02):** `src-tauri/Cargo.lock`, `uv.lock` — расхождение lock vs манифест, требует `--locked` gate.
- **Build cache:** `ui/tsconfig.tsbuildinfo`.
- **Diagnostics / artifacts (untracked):** `backend_*.log`, `*.png`, scratch `*.py` (fix.py, gen_l10n.py, process_audit.py, test_whisper.py …), `.pnpm-store/`, handoffs, daily-notes, `docs/public-launch/2026-07-17-*`.
- **User source changes: НЕТ.** Ни один незакоммиченный файл не является правкой продуктового кода. Reset/checkout/clean НЕ делаю.

---

## 2. Version matrix (P0-01 / OPUS-002)

| Источник | Версия | Дельта |
|---|---|---|
| pyproject.toml | `1.0.0-rc1` | ❌ отстаёт |
| kernel/__init__.py | `1.0.0-rc1` | ❌ отстаёт |
| src-tauri/Cargo.toml | `1.0.0-rc2` | — |
| src-tauri/Cargo.lock (kali-desktop) | `1.0.0-rc2` | — |
| src-tauri/tauri.conf.json | `1.0.0-rc2` | — |
| scripts/installer_premium.iss | `1.0.0-rc2` | — |
| mobile/pubspec.yaml | `0.1.0+1` | ❌ третья версия |

**Вывод:** version skew подтверждён по трём осям (Python rc1 / desktop rc2 / mobile 0.1.0). Единого source-of-truth нет. **FAIL — OPUS-002 обязателен.**

---

## 3. Installer identity (P0-01) — КРИТИЧЕСКАЯ ЛОВУШКА

| Поле | Значение |
|---|---|
| Файл | `dist_premium/installer/KALI-Premium-Setup-1.0.0-rc2.exe` |
| Size | 4 171 830 B |
| LastWrite | **2026-07-15 19:33:21** |
| SHA-256 | `E0A0B2A395DD5C1D6A42ED82E235AD6A7CB9768409D71F70ED0CC91E5C5B5C1A` (совпадает с историческим аудита) |
| Authenticode | **NotSigned** |
| bin parts | `-1.bin` 1.99 GB (19:24) · `-2.bin` 2.00 GB (19:32) · `-3.bin` 400 MB (19:33) |

**Skew по времени:** installer собран 19:33, последний TTS-фикс `ebcf981` попал в репо позже (per audit 21:38). Installer **предшествует** STT/TTS-фиксам, не подписан, версия rc2 при backend rc1. **DO-NOT-DISTRIBUTE подтверждён живым evidence. Evidence не перезаписывать до фикса pipeline.**

---

## 4. Runtime / startup evidence (P0-02) — из `%APPDATA%\Roaming\KALI\logs`

Логи: `kali-backend.log` (1.17 MB, последняя запись 2026-07-15 21:11). Диапазон записей широкий (июнь–июль), ниже — подтверждённые паттерны.

- **Blocking Tauri startup:** `src-tauri/src/lib.rs` — `start_backend()` вызывается в `setup` (стр. 243), `wait_for_backend_ready()` поллит со `sleep(250ms)` (стр. 82–87), `rt.block_on(backend::serve())` (стр. 251). Подтверждает синхронное ожидание backend.
- **Blocking voice prewarm в lifespan:** `kernel/main.py` стр. 555–606 — комментарий *«Sequential await here is intentional: startup blocks until both»*; `await asyncio.to_thread(load_models)` (TTS, стр. 580) + STT prewarm (стр. 595). Подтверждает P0-02.
- **Duplicate STT:** в логе «Whisper model loaded» идёт парами (стр. 64/65, 242/261, 324/330, 610/616) — два SpeechToText owner (`VoicePipeline.load_models` стр. 394 + prewarm стр. 595). Подтверждает P0-02.
- **ConnectionResetError / WinError 10054:** **330** совпадений (аудит: 329). PASS-как-описано.
- **Provider quota (429/rate_limit/quota):** 50 совпадений по расширенному паттерну (аудит: 12 чистых 429). Retry-storm риск подтверждён (P1-05).
- **TTS prewarm failures:** «TTS prewarm failed (non-fatal): Could not import module 'pipeline'» + «FFmpeg DLLs not found» — фрозен-бандл имел ML-проблемы в этой сессии.

---

## 5. Provider model (P0-05)

Retired `claude-sonnet-4-20250514` (retired 2026-06-15) захардкожен в **9 production-файлах**:
`kernel/models.py`, `kernel/routers/system.py`, `config/kali.yaml`, `src-tauri/src/backend/config.rs`,
`ui/src/components/Settings/Settings.tsx`, `ui/.../sections/LlmSettings.tsx`,
`mobile/lib/standalone/llm_client.dart`, `agents/coding/agent.py`, `agents/coding/scripts/agent.py`.
Единого registry нет. **FAIL — OPUS-301 обязателен.**

---

## 6. Updater (P0-03)

`src-tauri/src/backend/updater.rs`:
- тянет mutable манифест из `raw.githubusercontent.com/.../main/releases/latest.json` (стр. 19);
- проверяет только `sha256` из **того же изменяемого** манифеста (стр. 74, 439–440, 504);
- затем `Command::new(setup_exe).spawn()` авто-запуск (стр. 525–543).
- **Нет** asymmetric signature, embedded public key, downgrade/replay-защиты.

Компрометация GitHub → одновременная подмена EXE+hash → RCE. **FAIL — OPUS-202: отключить auto-install до signature chain.**

---

## 7. Tests (bounded)

| Suite | Результат | Прим. |
|---|---|---|
| Python `pytest -m core_loop` | **13 passed**, 1284 deselected, 14.4s | как baseline; background Shell_NotifyIconW НЕ проверял отдельно (P1-07) |
| UI vitest | не запускал в этой сессии (baseline 173/1 skip) | TODO при работе над OPUS |
| Flutter analyze/test | не запускал (baseline 155 + 21 issue) | TODO Phase C |
| Rust `--locked` | не запускал (ort-sys download gate P1-02) | TODO Phase A/E |
| voice suite | **НЕ запускал** — по контракту только bounded/per-file, найти hanging test | Phase E |

---

## 8. Delta-таблица к ожидаемому baseline

| Проверка | Ожидалось | Фактически | Статус |
|---|---|---|---|
| HEAD | e3db43c | e3db43c | PASS |
| origin sync | — | совпадает | PASS |
| version single-source | одна | rc1/rc2/0.1.0 (3) | FAIL |
| installer signed | — | NotSigned | FAIL |
| installer ≥ last commit | — | предшествует TTS-фиксам | FAIL |
| Tauri non-blocking boot | — | blocking (setup+poll) | FAIL |
| single STT owner | 1 | 2 | FAIL |
| lifespan lightweight | — | awaited TTS+STT prewarm | FAIL |
| retired model absent | 0 | 9 файлов | FAIL |
| updater signed chain | — | только SHA256 из mutable manifest | FAIL |
| core_loop | 13 | 13 | PASS |
| memory files | «не найдены» (session-start) | **найдены** в `~/.claude/.../memory/` (31 файл) | PASS (поправка к contract) |

Все P0-предпосылки аудита воспроизведены на живом state. Переходим к плану PHASE A (ожидает утверждения владельца).

---

## 9. Решения владельца (2026-07-17)

- **Signing (A9/OPUS-201):** cert **нет** → финальная сборка = `INTERNAL-UNSIGNED-DO-NOT-DISTRIBUTE` только для локальной проверки. Fail-closed gate реализуем полностью, но не ослабляем ради зелёного pipeline; публично не раздаём и prod-ready не называем.
- **Default Anthropic model (A5/OPUS-301):** `claude-sonnet-5`. Configurable в Settings; registry валидирует доступность; deprecation-тест запрещает retired ID.
- **UGC (A7/OPUS-304):** для alpha отключаем произвольный native-Python import (fail-closed), оставляем curated/signed allowlist + prompt-only. Произвольный native вернём после настоящего least-privilege sandbox-процесса.

---

## 10. PHASE A — блок 1 (OPUS-001 freeze + OPUS-002 version SoT) — DONE + REVIEW FIX

Ветка: `release/phase-a-desktop-alpha` (не main; не пушено).

**Workflow-driven (ultracode):**
1. Adversarial spec-review (3 линзы) → fail-open блокеры: case-sensitive frozen hash (UPPERCASE vs lowercase hexdigest), truthy `distributable`, missing/malformed release-status→fail-open, Cargo.lock `version = 4` на стр.3, BOM/CRLF в VERSION, rebuild-touch обходит staleness, gitignored installer невидим для `git status`. Спека усилена HARDENED-секцией.
2. TDD red-first → **42 failed + 26 errors** (реализации нет).
3. Implementer → guard в `publish_release.py` + `scripts/release/version.py`. Само-верификация.
4. Мутация FROZEN_HASH → ровно 9 frozen-тестов краснеют → load-bearing.
5. Коммиты: `44666ee` (governing docs) + `9fbc336` (deliverable).

### Независимый Codex review (после `9fbc336`) — 6 must-fix, закрыты одним commit

**Главная находка: clean-checkout dependency** — `test_check_detects_real_repo_skew` зависел от локального dirty `Cargo.lock` (working-tree rc2 vs committed rc1) → падал бы в чистом checkout. Удалён; version-тесты теперь целиком на tmp-fixture.

1. **Clean-checkout independence:** убран real-repo snapshot; добавлены fixture-регрессии (`test_check_passes_on_synced_fixture`, `test_check_detects_skew_on_fixture`).
2. **Dirty gate = полностью чистый worktree:** `_check_dirty` теперь валит на ЛЮБОЕ tracked/untracked изменение (backend/UI/mobile/build), не только version-пути. `RELEASE_TRACKED_PATHS` удалён. Тесты на dirty kernel/main.py, ui/src, build-script.
3. **Freeze-first:** guard после `_load_status` валидирует schema и при `distributable != true` возвращает `NOT_DISTRIBUTABLE` ДО чтения VERSION / `collect_assets` / hashing. Spy-тест доказывает `collect_assets`/`_sha256` не вызываются. `canonical_version` обязан == VERSION.
4. **sync loud-fail:** каждый writer доказывает замену ровно одного anchor, иначе `VERSION_SYNC_FAILED`; после записей обязательный `check(repo)`; CLI печатает `synced` только при convergence. Негативные тесты на отсутствующий anchor всех 6 источников.
5. **Diagnostic vs release-manifest:** `build_manifest(strict=True)` (CLI `manifest`) отказывает при `VERSION_SKEW`/`RELEASE_MANIFEST_DIRTY`; добавлен диагностический CLI `diagnose` (показывает skew/dirty, не отказывает).
6. Этот лог обновлён.

**Evidence (проверено мной):**
- RED перед fix: **16 failed** (ровно новые поведения).
- GREEN после fix: `tests/scripts` **101 passed**; `core_loop` **13 passed**.
- CLI `check` на реальном skew → exit 1 `VERSION_SKEW: pyproject rc1 != rc3`.
- Guard на реальном workspace → exit 1 **`NOT_DISTRIBUTABLE`** (до чтения ассетов — freeze-first подтверждён).
- Мутации: full-clean dirty заглушён → **9 porcelain-dirty тестов** краснеют (stat-based installer остаётся); cargo_lock sync-anchor заглушён → **ровно `[cargo_lock]`** краснеет, финальный `check(repo)` ловит молчаливый провал как backstop; freeze-first — spy-тест был RED при старом порядке «ассеты первыми».

Входные артефакты: `VERSION`=1.0.0-rc3, `release-status.json`{distributable:false, frozen rc2 sha, burned rc1+rc2}. **sync НЕ запускался** — реальные версии остаются skewed до пересборки (convergence в A3/A8).

### Коммиты блока 1 (ветка `release/phase-a-desktop-alpha`, запушена на origin; main нетронут)
- `44666ee` docs(release): governing docs.
- `9fbc336` feat(release): OPUS-001 freeze + OPUS-002 version SoT.
- `1a299bc` fix(release): close OPUS-001/002 review gaps (Codex review #1 — clean-checkout dependency + full-clean dirty + freeze-first + sync loud-fail + manifest strict).
- **`9d94564` fix(release): validate release status safeguards (Codex review #2 — строгая schema: distributable=bool, canonical semver, burned non-empty unique semver, frozen non-empty list[dict] с 64-hex sha).**

**Финальный статус блока 1: `tests/scripts` 113 passed · `core_loop` 13 passed · CLI check→VERSION_SKEW · guard на workspace→NOT_DISTRIBUTABLE (freeze-first).** Codex gate блока 1: **PASS** (владелец).

---

## 11. PHASE A — блок A3 / OPUS-101 (non-blocking Tauri boot) — ПЛАН + red-test design

Статус: **план написан, ждёт утверждения владельца.** Реализация НЕ начата (пауза перед рантаймом).

Adversarial-planning workflow (context7 Tauri v2 docs + 2 map-агента + 3 attack-линзы + синтез, 25 threats). Root cause подтверждён: блокирующий `setup()` стопорит Tauri event-loop → webview не рисуется (серое окно); axum :3006 стартует поздно; emit-before-listener теряется; per-process mutex не ловит второй запуск; child без Job Object → orphan.

Дизайн: pure `src-tauri/src/startup.rs` (std-only leaf, инъекция `HealthProbe/BackendSpawner/Clock` traits) → все A3-поведения unit-тестируемы фейками без Tauri/ort; lib.rs = тонкий адаптер. 11 unit-тестов (mutation-noted) + 8 live-протоколов (против dev-machine-masking). 4 commit-слайса. План: [`docs/superpowers/plans/2026-07-17-a3-opus-101-nonblocking-boot.md`](../superpowers/plans/2026-07-17-a3-opus-101-nonblocking-boot.md).

**5 open questions владельцу** (restart policy, Job Object scope, engine=rust bind-order, test-delay lever, degraded RU-copy) — гейтят финализацию тестов/импл. OPUS-102 (загрузка моделей) намеренно вне A3.
