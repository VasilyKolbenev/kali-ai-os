# Implementation Plans (advisor)

Generated by the `improve` skill (quick audit) on 2026-06-12, planned at commit
`f6cfb93`. Vasily chose ONE unified sequential plan over per-finding plans —
execute its stages strictly in order. Executors: read the plan fully before
starting, honor its STOP conditions, update your row when done.

> Note: this directory is the advisor-plans workspace. Long-form feature plans
> live elsewhere (`docs/superpowers/plans/`) — different convention, don't mix.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Harden long-term-memory chain + test baseline (6 stages) | P1 | M | — | DONE (2026-06-12, executed by advisor session per operator; Stage 2 adapted — LTM tests already existed, extended instead of created) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Stage map of plan 001 (for partial progress tracking)

| Stage | Finding | Size |
|-------|---------|------|
| 1 | model_downloader characterization tests | S |
| 2 | LongTermMemory characterization tests | S |
| 3 | Memory sanitization (prompt-injection fix) | S |
| 4 | Fire-and-forget task reference fix | S |
| 5 | HF-offline edge in checkpoint fallback | S |
| 6 | Storefront (Мастерская) characterization tests | M |

## Findings considered and rejected / deferred

- «vocos is legacy» (subagent claim) — REJECTED at vetting: vocos is the active
  F5 vocoder; the related offline-gate finding was kept but downgraded.
- Verification baseline for the FULL pytest suite — known issue DEV-1 (native
  access violation in teardown), already in the project backlog; not re-planned
  here. Targeted suites are the working baseline.
- Facts dedup/contradictions, 50-fact cap as stopgap — deliberately deferred to
  the project backlog round #13 (schema migration); see plan 001 maintenance
  notes.
- Instruction-pattern blacklists for facts — rejected as brittle; structural
  sanitization + framing chosen instead.
